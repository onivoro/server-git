{
  "name": "@onivoro/server-git",
  "version": "0.1.0",
  "dependencies": {
    "@nestjs/common": "^7.6.12",
    "@onivoro/server-process": "^1.23.0"
  },
  "scripts": {
    "test": "jest",
    "build": "tsc -p tsconfig.json",
    "release": "rm -rf dist && npm run build && npm version minor && cp package.json dist && cd dist && npm publish --access public"
  },
  "devDependencies": {
    "@types/jest": "^26.0.14",
    "@types/node": "^14.11.2",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.1",
    "typescript": "^4.0.3"
  }
}
